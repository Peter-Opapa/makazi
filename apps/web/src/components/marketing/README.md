# Marketing-only components

- `MarketingNav` — sticky, blurred, active-link aware (`usePathname`)
- `Footer` — 4-column, identical across all marketing pages
- `HeroSection`, `EyebrowBadge`, `FeatureCard`, `PricingTierCard`, `ComparisonTable`
- `FaqAccordion` — reused on Home/Pricing/Contact
- `TestimonialCard`, `PhoneMockupFrame`, `RoleTabSwitcher` (Home dashboard preview), `FounderCard`, `FinalCtaBand`
